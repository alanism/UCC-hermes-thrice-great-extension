# Phase 3 Contract Lock Audit

Status: C3.7 PASS

Date: 2026-06-30

## Lock scope

Locked specifications:

- SMC normalization and amendment authority;
- assessment receipt and semantic validation;
- CALM/PRESSURE pairing and pressure delta;
- task proposal, approval event, and transition result;
- parent brief;
- ledger entry/file, atomicity, recovery, retention, and deletion;
- registry, version, canonicalization, and migrations.

Schemas, validators, evaluators, renderers, and ledger runtime are not implemented by this lock.

## Field-ambiguity audit

| Area | Lock result |
|---|---|
| Contract/version identity | Exact SemVer wire strings and non-network URN `$id` pattern. |
| IDs | Prefix plus uppercase ULID; learner/actor conventions global. |
| Clocks | Exact RFC 3339 UTC milliseconds; injected, never generated by validators. |
| Hashes | SHA-256, lowercase 64-hex field convention; RFC 8785/JCS semantic bytes. |
| Missing/unknown | Missing, null, empty string, and empty array have distinct contract meanings. |
| References | Typed ID/version/hash/status/resolution object; no absolute path. |
| SMC | Every current Markdown field maps once; activation and amendment authority explicit. |
| Receipt totals | Exact event-derived formulas; timeout/skipped denominators fixed. |
| Receipt quality | clean/limited/degraded/void/suppressed precedence fixed. |
| AI use | AI role recorded; no AI-use policing; mastery evidence requirements explicit. |
| Pair forms | Exact or mutually registered equivalent; formulas and thresholds supplied by immutable policy. |
| Pressure delta | Canonical correct/presented delta; answered-only contrast separately named. |
| Partial/degraded pairs | Never produce pressure metrics. |
| Proposal actions/evidence | Action object and evidence-policy fields enumerated. |
| Approval | Exact actor/action/revision/hash/provenance and global key binding. |
| Replay | Exact semantic replay succeeds once; changed/cross-revision reuse conflicts. |
| Brief claims | measured/calculated/modeled/forecast/noted labels and source authority explicit. |
| Mastery language | Established only through accepted ledger decision under locked policy. |
| Ledger payload mode | Embedded versus typed content-addressed reference is explicit and exclusive. |
| Ledger atomicity | Lock, temp, flush, replace, directory flush, readback, and fault outcomes fixed. |
| Privacy deletion | Authorized atomic compaction plus content-free tombstone; backup limits explicit. |
| Migration | Adapters cannot invent missing identity/evidence/approval/mastery truth. |

No unresolved field-level ambiguity remains inside the locked core specifications.

## Doctrine audit

- No cheating, ghostwriting, or AI-writing detector is designed.
- AI-assisted output is useful when its role is clear; mastery claims require independent evidence.
- `ghostwriting_integrity_gate` remains excluded from public/default release.
- Canonical existing skills remain in the local profile; no skill was copied or packaged.
- `parent_workflow` remains a sanitized release candidate; `aria_workflow` remains local/private.
- Repository root remains non-installable; generated staging is the sole install payload.
- Hermes remains pinned and untouched.

## Human authorization record

The human owner authorized progression “through Phase 3 contract lock” on 2026-06-30 and supplied the production, source, architecture, privacy, and forbidden-feature doctrines used by these specifications. C3.7 records that authorization as the contract-specification lock authority.

The authorization does not decide C3.8 optional mock-adapter scope. That remains an explicit INCLUDE/EXCLUDE human fork before C1 can close.

## RED harness readiness

The specifications are precise enough to author T4.1–T4.5 and T4.9 fixtures/tests after R4.5. Task-board dependencies still prohibit claiming those RED tasks now. Required negative cases are mapped in `REGISTRY_DESIGN.md` and each contract's acceptance criteria.
