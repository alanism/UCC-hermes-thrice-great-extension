# Receipt: C3.7A

Agent: `codex-c3-registry-amend-01`

Completed: `2026-06-30T08:15:17Z`

## Result

PASS. The registry/versioning contract now has an exact closed machine-testable wire format sufficient for T4.1 fixture and RED-test design.

## Amendment evidence

- exact `ucc_contract_registry` root envelope and required fields;
- exact five-family map, family objects, version records, nullable states, validator/projection references, compatibility, migrations, fixtures, approvals, and extensions;
- lifecycle enum and request-dependent resolvability;
- artifact, version-projection, and external registry-snapshot hashing separated without self-reference;
- deterministic exact-version and migration resolution;
- stable registry/migration issue codes;
- one complete parseable positive example with all five families and a recomputed canonical projection hash;
- eight negative mutations covering every human-required invalid case.

The positive example parsed as JSON, its registry and decision IDs contain valid 26-character ULIDs, its five family keys match the v1 lock, and its SMC `canonical_hash` recomputed to `1c42944213233e21a915e549b6ebbc2c96a67094b3ab1ae1a8be185cdbaa97a4`.

## Scope

No test, fixture file, schema, registry runtime, validator, migration, plugin, ledger, skill, profile, product behavior, Hermes source, learner data, model call, or network/messaging action was created or changed.

## Governance

The task board recognizes C3.7A, all changed binding hashes are current, and authority, claim/state, and archive-hygiene checks pass. C1 remains PASS with registry wire state `locked_by_C3.7A`.

## Next task

T4.1 schema-registry and contract-version RED tests are unblocked.
