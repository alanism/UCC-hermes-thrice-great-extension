# Receipt: R4.0

Agent: `codex-r4-readiness-01`

Completed: `2026-06-30T07:18:25Z`

## Result

PASS. The native-Windows pytest and filesystem host canary completed with two pytest cases passing.

## Observed host behavior

- Long path: `PASS_FAIL_CLOSED`; a 306-character file path was rejected with WinError 206.
- Drive case: `PASS_SUPPORTED`; `C:` and `c:` identify the same file.
- Reserved name: `PASS_SUPPORTED`; `CON` was creatable as a directory, so future product guards must reject reserved device names explicitly.
- Junction/reparse: `PASS_SUPPORTED`; a junction could escape a candidate containment root and canonical resolution exposed its outside target.

All probes used synthetic temporary files. Junctions were removed before temporary-tree cleanup. No learner data, network action, model call, or Hermes source change occurred.

## TDD evidence

The pytest case first failed because `scripts/run_host_canary.py` did not exist, then passed after the minimum host probe was implemented. A second failure exposed the host's creatable-`CON` behavior; the canary was corrected to record that supported-but-unsafe capability rather than misclassify it as infrastructure failure.

## Next task

R4.1 dependency/runtime pinning.
