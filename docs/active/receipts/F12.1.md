# Receipt: F12.1

Agent: `codex-f12-final-01`

Completed: `2026-07-01T11:28:58Z`

## Result

PASS. `README.md`, `INSTALL.md`, and `docs/active/OWNER_RUNBOOK.md` now describe the proven generated-payload-only, native-Windows, synthetic-offline release boundary.

## Replay evidence

- A fresh temporary staging tree was generated from the allowlist builder.
- Native `hermes profile install <staging> --name ucc --yes` succeeded in a fresh isolated Hermes root.
- The guarded installed-config activation preserved the compact deny-all restrictions and enabled only the delivered plugin.
- Installed `hermes ucc doctor`, `validate --synthetic`, and `dry-run --synthetic` all exited zero.
- The dry run completed seven ordered stages and one isolated temporary ledger commit.
- A socket-connect sentinel recorded zero attempts; command envelopes reported zero model calls.
- All temporary install state was removed after verification.

## Compatibility lessons

- PowerShell variable names are case-insensitive; `$home` collides with read-only `$HOME`. The runbook uses `$HermesRoot` and `$InstalledProfile` only.
- Hermes 0.16.0 discovers plugin subcommands from the active `HERMES_HOME` before parsing `-p`; installed plugin commands therefore run as `hermes ucc ...` after profile-home activation.
- `HERMES_SAFE_MODE=1` is appropriate during local installation but suppresses plugin loading; it is removed before command verification while deny-all profile restrictions and the socket sentinel remain active.
- Native `hermes plugins enable` expands the installed YAML. This release uses a guarded single-token activation that preserves the doctor-verified compact restriction form.

No product implementation, Hermes source modification, real learner data, network action, or model call occurred.

## Next task

F12.2 is dependency-ready.
