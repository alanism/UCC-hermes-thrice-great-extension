# Receipt: R4.3

Agent: `codex-r4-readiness-01`

Completed: `2026-06-30T07:31:37Z`

## Result

PASS. Generated private data is ignored and a repository-local pre-commit hook independently rejects forbidden staged paths. Local Git configuration now sets `core.hooksPath=.githooks`.

## Synthetic sentinel proof

A synthetic JSON sentinel was created under ignored `outputs/r4-synthetic-private/`. A separate temporary Git index was initialized from `HEAD`; the real index remained untouched.

- `git check-ignore`: exit 0.
- ordinary `git add`: exit 1, ignored.
- forced add to temporary index: exit 0.
- staged-data checker: exit 3, rejected.
- installed pre-commit hook: exit 3, rejected.

The forced-add success proves the rejection was performed by the staged-file policy rather than relying only on `.gitignore`. The sentinel directory and temporary index were safely removed after the proof. No real or semi-real learner data was used.

## TDD evidence

Three canary tests first failed because the checker and hook did not exist. They pass after the minimum case-insensitive path policy and hook were installed.

## Next task

R4.4 mutation-runner meta-canaries.
