# Receipt: H1.2

Agent: `codex-bootstrap-autopilot-01`

Completed: `2026-06-29T21:30:46Z`

## Result

PASS after one contained Windows variable-name correction. Local native distribution install/update behavior is characterized in `docs/active/recon/PROFILE_DISTRIBUTION_INSTALL_RECON.md`.

## Evidence

Eight install assertions and five update assertions passed:

- target containment;
- override profile name;
- local source provenance;
- native skill detection/copy;
- SOUL/config copy;
- user-directory bootstrap;
- distribution-owned SOUL restoration;
- config preservation;
- memory preservation;
- structured distribution description.

## Incident and recovery

The first synthetic target was written to `C:\Users\alani\profiles\probe-one` because `$home` collided with PowerShell's read-only `$HOME`. The target was verified by exact path and synthetic manifest, checked for reparse points, and removed. The corrected probe used `$probeHome` and asserted target containment before continuing. No non-probe data was touched.

## Files changed

- claim and state log
- recon report
- this receipt
- ignored synthetic temporary files, removed after validation

No Hermes source or product/runtime file changed.

## Graph delta

None.

## Next

H1.3 custom distribution-owned plugin/schema/benchmark path characterization.
