# Receipt: R4.4

Agent: `codex-r4-readiness-01`

Completed: `2026-06-30T07:34:12Z`

## Result

PASS. The mutation runner distinguishes behavioral kills, equivalent survivors, and infrastructure errors.

## Meta-canary outcomes

- Behavior-changing arithmetic mutant: `KILLED` from pytest exit 1.
- No-op/equivalent mutant: `SURVIVED` from pytest exit 0.
- Abrupt process crash: `ERROR` from exit 70.
- Setup/collection failure: `ERROR` from pytest exit 4.
- Timeout: `ERROR` from the subprocess deadline.

Every mutant first runs a known-green baseline. Only pytest exit 1 after a green baseline is classified as `KILLED`; crashes, setup failures, baseline failures, and timeouts can never earn mutation credit.

## TDD evidence

The meta-canary test first failed because the runner did not exist, then passed after the minimum classifier and synthetic cases were implemented. The full meta run completes well below the one-minute inner-loop limit.

## Next task

R4.5 full readiness audit.
