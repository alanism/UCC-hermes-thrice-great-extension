# Receipt: P6.1

Agent: `codex-p6-green-01`

Completed: `2026-06-30T18:54:43Z`

## Result

PASS. The minimum public profile sources generate the canonical allowlisted staging payload and install through pinned Hermes into isolated arbitrary-name profiles. Repository-root installation is rejected by the acceptance boundary.

## Payload

The generated ignored `dist/hermes-thrice-great-profile/` contains exactly 13 files: `distribution.yaml`, `SOUL.md`, `config.yaml`, `.env.EXAMPLE`, eight approved `SKILL.md` files, and one approved skill reference. It contains no plugin, schema, benchmark, governance, private-data, secret, cache, state, or user-owned path.

## TDD and install evidence

- RED: canonical in-repo output failed under the legacy overlap guard.
- GREEN: exact canonical output is permitted while all other source/output overlaps remain rejected.
- RED: optional plugin/schema/benchmark directories copied merely because they existed.
- GREEN: optional directories now require explicit `distribution_owned` selection.
- Focused builder/profile lane: 15 passed, 4 deselected in 3.82 seconds.
- Pinned Hermes installed from generated staging as `ucc`, optional `thoth`, and `synthetic-arbitrary-name`, all contained in isolated temporary homes.
- Public source/payload contains no hardcoded `thoth`, secret, private name/path, or forbidden skill.

## Safety

Plugins remain disabled; all Hermes toolsets remain denied. The profile explicitly warns it is not safe to operate with learner data before later gates.

## Next task

P6.2 inert plugin manifest/skeleton and delivery proof.
