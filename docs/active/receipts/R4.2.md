# Receipt: R4.2

Agent: `codex-r4-readiness-01`

Completed: `2026-06-30T07:28:32Z`

## Result

PASS. Pinned Hermes mechanically enforced the offline deny-all readiness baseline in an isolated temporary home.

Unrestricted Hermes produced 27 tool definitions. With `agent.disabled_toolsets: ["*"]`, the restricted definition set was empty. `HERMES_SAFE_MODE=1` suppressed a synthetic user plugin and enabled synthetic MCP endpoint: zero plugins loaded, no plugin marker appeared, zero MCP servers were returned, and the socket sentinel recorded zero connection attempts.

The checkout remained clean at `2a5dc0ef3df433a36abed9ee544ea067d807c438`. Docker is unavailable, so no container-isolation claim is made. The enforceable baseline instead removes all model-exposed terminal, execution, filesystem, browser, web, messaging, and MCP tools.

## TDD evidence

The canary first failed because the verifier did not exist, then passed after the minimum isolated verifier was added.

## Operational correction

An exploratory PowerShell command used the reserved `$HOME` variable instead of a temporary-path variable and created `C:\Users\alani\config.yaml` with the synthetic deny-all configuration. The exact file was inspected, matched the just-created 121-byte sentinel and timestamp, and was immediately removed with a literal non-recursive path. The formal proof then used a correctly isolated temporary directory and a socket sentinel. No Hermes source or real Hermes profile file changed.

## Next task

R4.3 generated-private-data ignore/hook enforcement.
