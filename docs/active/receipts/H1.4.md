# Receipt: H1.4

Agent: `codex-bootstrap-autopilot-01`

Completed: `2026-06-29T23:36:15Z`

## Result

PASS.

Pinned Hermes 0.16.0 successfully discovered a native profile skill, opt-in loaded an inert user plugin, registered both supported command surfaces, installed identical payload under two arbitrary profile names, and read the characterized restriction keys.

## Evidence

- Exact staging payload equality test is green; inventory is an uninstalled sibling sidecar.
- Native `SKILL.md` discovery and Windows frontmatter/platform matching passed.
- Plugin discovery reported enabled with no load error.
- Synthetic CLI and slash command registrations were present.
- `ucc-probe` and `thoth-probe` payload hashes matched.
- `plugins.enabled`, `plugins.disabled`, and `agent.disabled_toolsets` read back exactly.
- Full analysis: `docs/active/recon/NATIVE_EXTENSION_RECON.md`.

## Scope

No product/plugin behavior, Hermes source, network, model, gateway, messaging, or learner data was used or changed.

## Next task

H1.5.
