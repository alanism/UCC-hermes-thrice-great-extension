# Receipt: T4.5

Agent: `codex-t4-red-01`

Completed: `2026-06-30T15:51:43Z`

## Result

RED PASS. Ledger envelope, hash-chain, replay, atomic-write, retention, deletion, and tombstone tests fail only because the ledger implementation is absent.

## Focused evidence

- Fixture/hash/mutation-contract lane: 3 passed, 22 deselected.
- Full focused lane: 22 failed, 3 passed in 0.17 seconds.
- Every behavioral failure is `LEDGER_IMPLEMENTATION_MISSING` for `hermes_thrice_great.contracts.ledger`.
- The initial invocation used Hermes' runtime without pytest; it was classified as environment setup, then rerun with the pinned repository `.venv` interpreter. It is not RED evidence.

## Coverage

Tests cover exact file/entry envelopes; payload/source/approval/brief references; canonical payload, entry, and ledger hashes; injected entry identity; first/null and subsequent previous-hash rules; append ordering; identical replay and changed-payload conflict; same-directory atomic write faults; partial-temp exclusion; retention/deletion/tombstone shapes; request-before-tombstone history; no in-place erasure; tombstone privacy; inert extensions; unknown fields; and stable issue codes.

Six behavior-changing mutation probes must be `KILLED`: previous hash, changed replay, canonical hash, entry type, tombstone leak, and write-fault success. The key-order control must survive/skip; crash/setup/timeout remain `ERROR`.

No schema, ledger implementation, plugin, privacy implementation, or product behavior was added.

## Next task

T4.6 Windows traversal/reparse/redaction/retention/commit-eligibility RED tests.
