# Receipt: H1.3

Agent: `codex-bootstrap-autopilot-01`

Completed: `2026-06-29T21:33:12Z`

## Result

CHARACTERIZATION PASS / MILESTONE BLOCKED.

Custom plugin/schema/benchmark paths install and update correctly, and user-owned secret/local paths are excluded or preserved. However, the installer copies every other non-excluded top-level source entry regardless of `distribution_owned`.

## Evidence

- Nine install assertions passed.
- Six update assertions passed.
- Installed manifest retained the declared custom paths.
- Pinned source inspection confirms `_copy_dist_payload()` does not consult `manifest.owned_paths()`.
- Full analysis: `docs/active/recon/DISTRIBUTION_PAYLOAD_RECON.md`.

## Files changed

- claim and state log
- recon report
- this receipt
- ignored synthetic temporary files, removed after validation

No Hermes source or product/runtime implementation changed.

## Graph delta

Required but not applied. The direct-repository distribution boundary is invalid under the pinned installer.

## Stop condition

Triggered: required dependency cannot be resolved without changing the build plan's packaging architecture.

## Next recommendation

Human chooses between a dedicated distribution-only repository (cleanest) and a generated allowlisted staging/release surface. After authority/graph revision, resume at H1.4.
