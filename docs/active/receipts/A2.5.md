# Receipt: A2.5

Agent: `codex-bootstrap-autopilot-01`

Completed: `2026-06-29T23:44:57Z`

## Result

EXPECTED RED.

Six claim/state synchronization tests were captured before implementation. They specify a valid active claim and mutations for missing required fields, stale active-plan hash, unknown task ID, missing DONE event, and duplicate active claims.

All six fail because `scripts/check_claim_state.py` does not exist. No schema or checker implementation was added during this task.

## Next task

A2.6 implements the claim schema and synchronization checker.
